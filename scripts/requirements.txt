# Heritage Parser Dependencies
# Install with: pip install -r requirements.txt

# RDF library for graph manipulation and TTL serialization
rdflib>=6.0.0

# XML parsing with XPath support
lxml>=4.9.0

# SPARQL endpoint queries for Wikidata/DBpedia enrichment
SPARQLWrapper>=2.0.0

# HTTP requests (dependency of SPARQLWrapper, explicit for clarity)
requests>=2.28.0

# Load environment variables from .env file
python-dotenv>=1.0.0
